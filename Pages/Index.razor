@page "/"
@inject StockController stockController

<Table TableName="In Stock" T="StockObject" Data="stockObjects" />

@code {
    List<StockObject> stockObjects = new List<StockObject>();
    
    protected async override Task OnInitializedAsync()
    {
        stockObjects = await stockController.Get();
    }
}
